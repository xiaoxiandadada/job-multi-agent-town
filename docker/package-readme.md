# Job Agent Studio

Container build placeholder used to cache Python dependencies independently
from documentation changes. The full project README is copied into the final
image after dependency installation.
